# 仓库 - 电区划
automate_silo_autobuild_1 = {
	category = "planet_automation_silo"
	
    emergency = yes
	available = {
		has_planet_flag = bca_pf_can_build_extra_zones_for_storage
		bca_has_minerals_to_build = yes
		exists = owner
        num_districts = { type = district_generator value < 1 }
        num_districts = { type = district_hab_energy value < 1 }
	}

	prio_districts = {
		district_generator
		district_hab_energy
    }
}

# 仓库 - 矿区划
automate_silo_autobuild_2 = {
	category = "planet_automation_silo"
	
    emergency = yes
	available = {
		has_planet_flag = bca_pf_can_build_extra_zones_for_storage
		bca_has_minerals_to_build = yes
		exists = owner
        num_districts = { type = district_mining value < 1 }
        num_districts = { type = district_hab_mining value < 1 }
	}

	prio_districts = {
		district_mining
		district_hab_mining
    }
}

# 仓库 - 农区划
automate_farming_autobuild = {
	category = "planet_automation_silo"
	
    emergency = yes
	available = {
		has_planet_flag = bca_pf_can_build_extra_zones_for_storage
		bca_has_minerals_to_build = yes
		exists = owner
        num_districts = { type = district_farming value < 1 }
	}

	prio_districts = {
		district_farming
    }
}
